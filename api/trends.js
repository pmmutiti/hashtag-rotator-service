
import axios from 'axios'; import cheerio from 'cheerio'; export default async function handler(req, res) { const region = req.query.region || 'kenya'; const url = `https://trends24.in/${region}/`; try { const { data } = await axios.get(url); const $ = cheerio.load(data); const hashtags = []; $('ol.trend-card li a').each((_, el) => { const text = $(el).text().trim(); if (text.startsWith('#')) { hashtags.push(text); } }); res.status(200).json({ hashtags }); } catch (error) { res.status(500).json({ error: 'Failed to fetch hashtags', details: error.message }); } } 
